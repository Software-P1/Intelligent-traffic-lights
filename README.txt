Three programs are in this file the first is a simulation of a traffic light. The other two programs were using machine learning. 

The two program base on machine learning can compile but do not function properly.
